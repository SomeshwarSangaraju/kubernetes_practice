apiVersion: v1
kind: Pod
metadata:
  name: annotations
  labels:
    project: roboshop
    environment: dev
    purpose: annotations
spec:
  containers:
  - name: nginx
    image: nginx
    resources:
      requests:
        cpu: "64m"
        memory: "100mi"  
      limits:
        cpu: "100m"
        memory: "150mi"